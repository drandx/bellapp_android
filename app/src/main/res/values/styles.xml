<resources>

    <!-- Navigation drawer with material design-->
    <style name="MyMaterialTheme" parent="MyMaterialTheme.Base">

    </style>

    <style name="MyMaterialTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="BellappTextViewHeader">
        <item name="android:textColor">@color/gray_scale_palette_black</item>
        <item name="fontPath">fonts/Roboto-Regular.ttf</item>
        <item name="android:textSize">25dp</item>
    </style>
    <style name="BellappTextViewTitle">
        <item name="fontPath">fonts/Roboto-Black.ttf</item>
        <item name="android:textSize">20dp</item>
    </style>
    <style name="BellappTextViewSubTitle">
        <item name="fontPath">fonts/Roboto-Light.ttf</item>
        <item name="android:textSize">15dp</item>
    </style>

</resources>

